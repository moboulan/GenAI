# Orchestrator

Agent runtime responsible for intent detection, tool routing, rule evaluation, and response formatting.

## TODO

- `agent/config/`: Prompt templates, tool definitions, and guardrail YAML.
- `agent/main.py`: Entry point that wires LangChain/Semantic Kernel with KPI, RAG, and rules tools.
- `rules/`: Machine-readable decision trees distilled from logigrammes.
